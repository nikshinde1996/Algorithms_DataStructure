#include<bits/stdc++.h>

using namespace std;

int longestSubSequence(int *arr,int n){
    int len = 0;
    for(int i=0;i<n;i++){
      cout<<arr[i]
    }
    return len;
}

int main(){
    int arr[10] = {5, 21, 10, 13, 50, 34,87};
    cout<<longestSubSequence(arr,sizeof(arr)/sizeof(arr[0]));
}
